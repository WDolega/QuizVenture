import classes from './Theme.module.css';

const Reservoirs = () => {
	return (
		<div className={`${classes.theme} ${classes.reservoirs}`}>
			<p>Reservoirs</p>
		</div>
	);
};

export default Reservoirs;
